# SketchAI UGC Webhook Environment Configuration
# Copy this file to .env and fill in your actual values

# Required: Webhook API Key (generate a secure random key)
WEBHOOK_API_KEY=your_secure_api_key_here

# Required: Email Configuration
MODERATION_EMAIL=moderation@sketchai.app
ADMIN_EMAIL=admin@sketchai.app

# Required: SMTP Configuration for email notifications
SMTP_HOST=smtp.your-provider.com
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Optional: Database Configuration
DATABASE_PATH=./sketchai_reports.db

# Optional: Server Configuration
PORT=3000

# Security Notes:
# 1. Never commit .env files to version control
# 2. Use strong, randomly generated API keys
# 3. Rotate keys regularly
# 4. Use environment-specific keys for different deployments